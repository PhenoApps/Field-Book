<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/fb_color_background"
    app:layout_constraintWidth_default="percent">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:minHeight="?attr/actionBarSize"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbarBottom"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:minHeight="?attr/actionBarSize"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent">

        <androidx.compose.ui.platform.ComposeView
            android:id="@+id/toolbarBottomCompose"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

    </androidx.appcompat.widget.Toolbar>

    <!--
        Never displayed: the constraints park it past the bottom-right corner of the parent.
        It exists only to own a SurfaceTexture for the UVC driver, and its LayoutParams are
        replaced at runtime with the camera's own pixel dimensions in
        UsbCameraTraitLayout.updatePreviewSize. px is the correct unit for a capture surface -
        converting these to dp would change the preview buffer geometry on every non-mdpi device.
    -->
    <com.serenegiant.widget.UVCCameraTextureView
        android:layout_width="640px"
        android:layout_height="480px"
        android:visibility="visible"
        android:id="@+id/collect_activity_uvc_tv"
        app:layout_constraintTop_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="parent"
        tools:ignore="PxUsage" />

    <!--
        Capped rather than wrap_content: one row is added per selected info bar attribute, and
        users routinely pick four to six. Unbounded, the rows push svTraitContainer (the only
        flexible element in the chain) to zero height in a short window and the data entry area
        disappears. The recycler already scrolls internally.
    -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/act_collect_infobar_rv"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:scrollbars="vertical"
        app:layout_constraintHeight_default="wrap"
        app:layout_constraintHeight_max="@dimen/fb_collect_infobar_max_height"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/toolbar" />

    <com.fieldbook.tracker.views.TraitBoxView
        android:id="@+id/act_collect_trait_box"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/act_collect_infobar_rv"
        app:layout_constraintBottom_toTopOf="@id/act_collect_range_box"/>

    <com.fieldbook.tracker.views.RangeBoxView
        android:id="@+id/act_collect_range_box"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/act_collect_trait_box"
        app:layout_constraintBottom_toTopOf="@id/act_collect_input_view"/>

    <com.fieldbook.tracker.views.CollectInputView
        android:id="@+id/act_collect_input_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="2dp"
        android:padding="2dp"
        app:layout_constraintBottom_toTopOf="@id/svTraitContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/act_collect_range_box" />

    <!--
        The trait entry area. constraintHeight_min gives it a floor so the chrome above cannot
        squeeze it out of existence in a short window; it scrolls internally once it hits the
        floor.

        fillViewport stays false: traitHolder has gravity="center", so filling the viewport
        would stretch the holder and centre every trait layout in the leftover space instead of
        stacking it directly under the input view.
    -->
    <ScrollView
        android:id="@+id/svTraitContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:fillViewport="false"
        android:isScrollContainer="true"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintHeight_min="@dimen/fb_collect_trait_container_min_height"
        app:layout_constraintBottom_toTopOf="@id/toolbarBottom"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/act_collect_input_view">

        <LinearLayout
            android:id="@+id/traitHolder"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"/>

    </ScrollView>

    <View
        android:id="@+id/lockOverlay"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@android:color/transparent"
        android:visibility="gone"
        android:clickable="true"
        app:layout_constraintTop_toTopOf="@id/act_collect_input_view"
        app:layout_constraintBottom_toBottomOf="@id/toolbarBottom"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
