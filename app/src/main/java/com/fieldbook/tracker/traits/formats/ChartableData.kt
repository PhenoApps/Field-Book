package com.fieldbook.tracker.traits.formats

/**
 * Trait format definitions implement this when their observation value data can be plotted in a chart.
 *
 * Future: Like presenters/coders, this can be expanded to allow individual trait data parsing in the
 * respective format definition files.
 */
interface ChartableData
